dist
.eslintcache

*.iml
.idea/
# .vscode/
.DS_Store
*.log*
*.debug*

# yarn berry PnP zero-install
.yarn/*
!.yarn/cache
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
!.yarn/sdks

# This is the Yarn build state; it's local to each clone
/.yarn/build-state.yml

# This is the Yarn install state cache, it can be rebuilt anytime
/.yarn/install-state.gz

# other package manager's stuff
.yarnrc
.npmrc
node_modules
package-lock.json
pnpm-lock.yaml

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
